(function(){const app=document.getElementById('compare-app');if(!app||!window.GonzoSnakeData)return;const{species}=window.GonzoSnakeData;const params=new URLSearchParams(location.search);let chosen=(params.get('species')||'').split(',').filter(id=>species.some(s=>s.id===id)).slice(0,3);const esc=t=>String(t).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const photo=s=>s.image?` style="background-image:linear-gradient(180deg,transparent,rgba(5,8,7,.9)),url('../${esc(s.image)}')"`:'';const section=(t,c)=>`<details open><summary>${t}</summary><div>${c}</div></details>`;function card(s){return`<article class="compare-card"><div class="snake-photo${s.image?'':' placeholder'}"${photo(s)}><span>${s.image?'':'Zdjęcie w przygotowaniu'}</span></div><div class="compare-card-copy"><h2><em>${esc(s.scientificName)}</em><br>${esc(s.commonName)}</h2><p>${esc(s.shortDescription)}</p>${section('Warunki i skala',`<p><strong>Długość dorosłego:</strong> ${s.adultLengthMin}–${s.adultLengthMax} cm</p><p><strong>Rekomendowane terrarium:</strong> ${esc(s.recommendedEnclosure)}</p><p><strong>Typ:</strong> ${esc(s.enclosureType)}</p>`)}${section('Mikroklimat',`<p><strong>Temperatura:</strong> ${esc(s.temperatureModel)}</p><p><strong>Wilgotność:</strong> ${esc(s.humidityModel)}</p><p><strong>Wentylacja:</strong> ${esc(s.ventilationImportance)}</p>`)}${section('Codzienność',`<p><strong>Aktywność:</strong> ${esc(s.activityLevel)} (${esc(s.activityPeriod)})</p><p><strong>Obsługa:</strong> ${esc(s.handlingPotential)}</p><p><strong>Karmienie:</strong> ${esc(s.typicalPrey)}</p><p><strong>Koszt startu:</strong> ${esc(s.startupCost)}</p>`)}${section('Plusy i wyzwania',`<p><strong>Plusy:</strong> ${esc(s.advantages.join('; '))}</p><p><strong>Wyzwania:</strong> ${esc(s.challenges.join('; '))}</p>`)}<p class="legal-note"><strong>Prawo:</strong> ${esc(s.legalNotes)}</p></div></article>`}function picker(){const b=species.map(s=>`<button type="button" class="species-choice${chosen.includes(s.id)?' selected':''}" data-id="${s.id}" aria-pressed="${chosen.includes(s.id)}"><em>${esc(s.scientificName)}</em><span>${esc(s.commonName)}</span></button>`).join('');app.innerHTML=`<section class="compare-picker"><h2>Wybierz 2 lub 3 gatunki</h2><p>Porównywarka działa na jednej, wspólnej bazie danych. Nie zastępuje osobnego przygotowania dla konkretnego osobnika.</p><div class="species-picker" aria-label="Lista gatunków">${b}</div><div class="quiz-actions"><button class="snake-button" type="button" data-action="compare" ${chosen.length<2?'disabled':''}>Porównaj wybrane (${chosen.length}/3)</button><button class="snake-button secondary" type="button" data-action="clear" ${chosen.length?'':'disabled'}>Wyczyść</button></div></section>`}function comparison(){const selected=chosen.map(id=>species.find(s=>s.id===id));app.innerHTML=`<section class="comparison-intro"><p class="snake-eyebrow">PORÓWNANIE GATUNKÓW</p><h2>Różnice, które naprawdę zmieniają hodowlę</h2><p>Parametry są opisane jako praktyczny punkt wyjścia. Nie przenoś automatycznie ustawień między gatunkami — szczególnie między <em>Corallus caninus</em> a <em>Morelia viridis</em>.</p><button class="text-link" type="button" data-action="edit">Zmień wybór</button></section><div class="comparison-grid">${selected.map(card).join('')}</div><aside class="why-not"><h2>Jak czytać to porównanie?</h2><p>Największe różnice zwykle dotyczą finalnej skali terrarium, marginesu błędów mikroklimatu, karmówki i tego, czy gatunek jest realnie do obserwacji czy do okazjonalnej obsługi.</p></aside><div class="result-actions"><button class="snake-button secondary" type="button" data-action="share">Udostępnij porównanie</button><a class="snake-button secondary" href="../jaki-waz-dla-ciebie/">Przejdź do quizu</a></div>`}async function share(){try{const u=location.href;if(navigator.share)await navigator.share({title:'Porównywarka węży — Gonzo Exotics',url:u});else if(navigator.clipboard){await navigator.clipboard.writeText(u);alert('Link do porównania skopiowany.')}}catch(_){}}app.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;const id=b.dataset.id;if(id){chosen=chosen.includes(id)?chosen.filter(x=>x!==id):(chosen.length<3?[...chosen,id]:chosen);picker();return}if(b.dataset.action==='compare'){history.replaceState(null,'',`?species=${encodeURIComponent(chosen.join(','))}`);comparison()}if(b.dataset.action==='clear'){chosen=[];history.replaceState(null,'',location.pathname);picker()}if(b.dataset.action==='edit')picker();if(b.dataset.action==='share')share()});chosen.length>=2?comparison():picker()})();

