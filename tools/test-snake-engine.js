const fs=require('fs');const vm=require('vm');const ctx={window:{},console};vm.createContext(ctx);for(const f of ['assets/js/snake-data.js','assets/js/snake-engine.js'])vm.runInContext(fs.readFileSync(`snake-tools/${f}`,'utf8'),ctx,{filename:f});const {species}=ctx.window.GonzoSnakeData;const {rank,scoreSpecies}=ctx.window.GonzoSnakeEngine;let failed=0;const check=(condition,message)=>{if(!condition){failed++;console.error(`FAIL: ${message}`)}else console.log(`PASS: ${message}`)};const required=['id','scientificName','commonName','adultLengthMin','adultLengthMax','recommendedEnclosure','temperatureModel','humidityModel','ventilationImportance','feedingRequirements','startupCost','ongoingCost','cites','sources','lastVerified','hardExclusions'];for(const s of species){for(const key of required)check(s[key]!==undefined,`${s.id} has ${key}`);check(s.sources.length>=2,`${s.id} has source trail`);check(!s.needsReview,`${s.id} is not marked for review`)}const profiles=[['A',{experience:'beginner',space:120,size:'medium',handling:'regular',type:'any',budget:2500,climate:1,prey:'small',breeding:'no',avoid:[]}],['B',{experience:'beginner',space:90,size:'small',handling:'occasional',type:'terrestrial',budget:1500,climate:1,prey:'small',breeding:'no',avoid:['large-snake','large-enclosure']}],['C',{experience:'limited',space:180,size:'large',handling:'occasional',type:'semi',budget:4500,climate:2,prey:'rats',breeding:'maybe',avoid:[]}],['D',{experience:'advanced',space:180,size:'medium',handling:'observation',type:'arboreal',budget:7000,climate:4,prey:'rats',breeding:'no',avoid:[]}],['E',{experience:'advanced',space:450,size:'giant',handling:'occasional',type:'any',budget:15000,climate:4,prey:'large',breeding:'yes',avoid:[]}],['F',{experience:'experienced',space:120,size:'small',handling:'occasional',type:'terrestrial',budget:2500,climate:2,prey:'small',breeding:'no',avoid:[]}]];for(let i=0;i<24;i++){const base=profiles[i%profiles.length][1];profiles.push([`G${i}`,{...base,avoid:i%2?['microclimate']:[],budget:[1500,2500,4500,7000][i%4]}])}for(const[name,a]of profiles){const out=rank(species,a);check(out.length>0,`${name} returns at least one recommendation`);check(out.every(x=>Number.isFinite(x.score)&&!x.blocked),`${name} returns finite, unblocked scores`)}const get=id=>species.find(s=>s.id===id);const a=profiles[0][1],d=profiles[3][1],e=profiles[4][1];check(scoreSpecies(get('python-bivittatus'),a).blocked,'A blocks Python bivittatus for first snake / space');check(scoreSpecies(get('corallus-caninus'),a).blocked,'A blocks Corallus caninus for first snake');const arb=rank(species,d).slice(0,5).map(x=>x.species.id);check(arb.includes('morelia-viridis')||arb.includes('corallus-caninus'),'D surfaces an advanced arboreal species');check(rank(species,e).slice(0,5).some(x=>x.species.id==='python-bivittatus'),'E can surface Python bivittatus with giant-space profile');if(failed){console.error(`\n${failed} checks failed.`);process.exit(1)}console.log(`\nAll ${species.length} species and ${profiles.length} profiles passed.`);

